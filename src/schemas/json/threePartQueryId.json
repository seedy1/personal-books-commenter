
{
    "$schema": "http://json-schema.org/schema",
    "$id": "ThreeIds",
    "type": "object",
    "additionalProperties": false,
    "required": ["bookId", "id", "commentId"],
    "properties": {
        "bookId": {"type": "integer"},
        "id": {"type": "integer"},
        "commentId": {"type": "integer"}
    }
}