import {expect} from "chai";
// unit validation test
describe('Book', function(){
    describe('book model', function(){
        it('should connect to the database', async function(){
            expect('a').to.equal('a');
        })

        it('should get the count if any', async function(){
            expect('a').to.equal('a');

        })

        it('should get first book title', async function(){
            expect('a').to.equal('a');

        })
    })
})